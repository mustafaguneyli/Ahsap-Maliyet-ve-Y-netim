/**
 * Kapı kasası hesaplama sınırı.
 * Faz 1A: gerçek maliyet/satış formülü implement edilmez.
 */
export class DoorFrameCalculator {
  calculate(_input: unknown): never {
    throw new Error(
      'DoorFrameCalculator henüz implement edilmedi. Faz 1A yalnızca mimari sınırı oluşturur.',
    );
  }
}
